import { createMuiTheme } from '@material-ui/core/styles';

export const myTheme = createMuiTheme({
  primary: {
    main: '#674ea7ff',
    light: '#de801f',
    contrastText: '#fff',
    dark: '#000',
  }
});